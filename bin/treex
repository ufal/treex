#!/usr/bin/env perl
use strict;
use warnings;
use Treex::Core::Run;
use List::MoreUtils qw(first_index);
my $idx   = first_index {$_ eq '--'} @ARGV;
my @files = ($idx == -1) ? () :  splice @ARGV, $idx+1;
my $app   = Treex::Core::Run->new_with_options({filenames=>\@files});
$app->execute();